import React from "react";
import ReactDOM from "react-dom";

// Improt the already built component from the location
import App from "./App";

// What do i want to render and where to render that's written in id
ReactDOM.render(<App/>,document.querySelector('#root'))